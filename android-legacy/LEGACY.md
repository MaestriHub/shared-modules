# Legacy Module (Android)

This folder is kept for backward compatibility with existing Android integrations.

Status:
- Legacy (maintenance mode)
- New DTO development should go to generated module:
  - `modules/KotlinDTOs`

Migration target:
- Replace imports/types from `android-legacy/shared/src/main/kotlin/shared/dto` with generated models from `modules/KotlinDTOs`.
