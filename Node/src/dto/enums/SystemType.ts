import { Price } from "../primitives/Price"

type SystemType = Ios | Android | Chrome | Edge | Firefox | Opera | Safari 

interface Ios {
    version: string
}

interface Android {
    version: string
}

interface Chrome {
    version: string
}

interface Edge {
    version: string
}

interface Firefox {
    version: string
}

interface Opera {
    version: string
}

interface Safari {
    version: string
}