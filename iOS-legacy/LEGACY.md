# Legacy Module (iOS)

This folder is kept for backward compatibility with existing iOS/Vapor integrations.

Status:
- Legacy (maintenance mode)
- New DTO development should go to generated module:
  - `modules/SwiftDTOs`

Migration target:
- Replace imports/types from `iOS-legacy/DTOs` with generated models from `modules/SwiftDTOs`.
